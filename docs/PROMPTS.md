# ERP Management System — AI Prompt Library

> **Version:** 1.0.0  
> **Purpose:** Ready-to-use AI prompts for every development stage  
> **Last Updated:** 2026-07-07

---

## How to Use This File

1. Find the prompt that matches your current task
2. Replace all `[PLACEHOLDERS]` in square brackets with your specific values
3. Paste into your AI assistant (Claude, Gemini, ChatGPT, Copilot)
4. Add context: paste relevant existing code when prompted to "see existing pattern"
5. Review all generated code before accepting it

---

## Table of Contents

- [Phase 0 — Planning Prompts](#phase-0--planning-prompts)
- [Phase 1 — Setup Prompts](#phase-1--setup-prompts)
- [Phase 2 — Authentication Prompts](#phase-2--authentication-prompts)
- [Phase 3 — Entity CRUD Prompts](#phase-3--entity-crud-prompts)
- [Phase 4 — Transaction Module Prompts](#phase-4--transaction-module-prompts)
- [Phase 5 — Dashboard & Reports Prompts](#phase-5--dashboard--reports-prompts)
- [General Debugging Prompts](#general-debugging-prompts)
- [Code Review Prompts](#code-review-prompts)
- [Documentation Prompts](#documentation-prompts)

---

## Phase 0 — Planning Prompts

### P0-1: Architecture Validation
```
I'm designing an ERP system with the following architecture:

Tech Stack:
- Frontend: React 18 + Vite + TypeScript + Tailwind CSS + Shadcn UI
- Backend: Supabase (Auth + PostgreSQL + PostgREST)
- State: TanStack Query (React Query v5) for server state
- Forms: React Hook Form + Zod
- Routing: React Router v6

Modules: Authentication, Dashboard, Products, Customers, Suppliers, 
         Purchases, Sales, Reports

Core Business Rules:
- Purchase completion → auto-increase product stock
- Sale completion → auto-decrease product stock
- Stock cannot go negative

Please identify:
1. Any architectural concerns or anti-patterns
2. Missing infrastructure I should plan for
3. Potential scaling bottlenecks for a 50-100 concurrent user system
4. Any Supabase-specific limitations I should be aware of
```

### P0-2: Database Schema Review
```
Review this planned PostgreSQL schema for an ERP system.

Tables and key fields:
- users (id UUID PK, full_name, email, role, created_at)
- products (id UUID PK, user_id FK, name, sku UNIQUE, unit_price, cost_price, stock_qty, min_stock)
- customers (id UUID PK, user_id FK, name, email, phone, address)
- suppliers (id UUID PK, user_id FK, name, email, phone, address)
- purchases (id UUID PK, user_id FK, supplier_id FK, status, total_amount, purchase_date)
- purchase_items (id UUID PK, purchase_id FK, product_id FK, quantity, unit_price, total_price)
- sales (id UUID PK, user_id FK, customer_id FK, status, total_amount, sale_date)
- sale_items (id UUID PK, sale_id FK, product_id FK, quantity, unit_price, total_price)

Please identify:
1. Normalization issues
2. Missing important fields
3. Indexing recommendations
4. Referential integrity concerns
5. Supabase RLS compatibility
```

---

## Phase 1 — Setup Prompts

### P1-1: Project Scaffold Command Sequence
```
Give me the exact shell commands (in order) to scaffold a new React + Vite + 
TypeScript project with Tailwind CSS and Shadcn UI.

Requirements:
- Project name: erp-management-system
- Vite template: react-ts
- Tailwind CSS v3 with PostCSS
- Shadcn UI initialization (using the 'new-york' style, CSS variables)
- Install: @supabase/supabase-js, @tanstack/react-query, react-router-dom,
           react-hook-form, @hookform/resolvers, zod, lucide-react
- Install dev: @types/node

Show each command separately. Do not combine unrelated steps.
```

### P1-2: Supabase Client Setup
```
Generate the Supabase client configuration file for a React + TypeScript project.

File: src/lib/supabase.ts

Requirements:
- Import createClient from @supabase/supabase-js
- Read URL and ANON_KEY from import.meta.env (Vite env vars)
- Export typed client using Database type from src/types/database.types.ts
- Export a helper function: getAuthHeader() that returns the current user's JWT

The Database type will be auto-generated by Supabase CLI and placed at 
src/types/database.types.ts (don't generate the type, just reference it).
```

### P1-3: TanStack Query Setup
```
Generate the TanStack Query (React Query v5) client configuration.

File: src/lib/queryClient.ts

Requirements:
- Create a QueryClient with these defaults:
  - staleTime: 5 * 60 * 1000 (5 minutes)
  - gcTime: 10 * 60 * 1000 (10 minutes)  
  - retry: 1 (on error)
  - refetchOnWindowFocus: true
- Export the queryClient instance
- Export a QueryClientProvider wrapper component for use in main.tsx
```

### P1-4: CSS Design Tokens
```
Generate a CSS file with custom properties implementing the Modern Glass ERP 
design system for a dark, professional SaaS dashboard.

File: src/index.css (to be placed in the :root selector)

Include CSS custom properties for:
Background colors: base (#080C14), surface-100/200/300/400 (dark blue-grays)
Glass layers: glass-bg (rgba whites at 5%, 8%, 12%), glass-border
Primary brand: primary-400/500/600/700 (blues: #60A5FA, #3B82F6, #2563EB, #1D4ED8)
Semantic: success (greens), warning (ambers), danger (reds), info (sky blues)
Text: primary (#F1F5F9), secondary (#94A3B8), tertiary (#64748B), muted (#475569)
Borders: subtle (rgba white 6%), default (10%), strong (18%)
Shadows: xs through xl + glow variants (primary, success, danger)
Border-radius: sm(6px) md(8px) lg(12px) xl(16px) 2xl(24px) full(9999px)

Also add the @import for Inter and JetBrains Mono from Google Fonts.
Add base styles: html/body background, font-family Inter, color text-primary,
smooth scroll, and antialiased text rendering.
```

### P1-5: Tailwind Config
```
Generate a tailwind.config.ts for a React + Vite project that:

1. Extends the default theme with custom colors matching the CSS variables 
   defined in index.css (use hsl(var(--variable)) pattern for Shadcn compatibility)
2. Adds custom font families: 'Inter' for sans, 'JetBrains Mono' for mono
3. Adds custom border-radius values: sm, md, lg, xl, 2xl mapping to CSS vars
4. Adds custom box-shadow values: xs through xl, plus glow-primary, glow-success, glow-danger
5. Includes animation utilities for: shimmer, fadeIn, slideDown
6. Content paths: ["./index.html", "./src/**/*.{ts,tsx}"]
7. Uses the Shadcn darkMode class strategy
```

---

## Phase 2 — Authentication Prompts

### P2-1: Auth Context
```
Generate a React AuthContext using Supabase Auth.

File: src/context/AuthContext.tsx

Requirements:
- Context value type: { user: User | null, session: Session | null, 
  isLoading: boolean, signOut: () => Promise<void> }
- Initialize with supabase.auth.getSession() on mount
- Subscribe to supabase.auth.onAuthStateChange
- Unsubscribe on cleanup
- signOut calls supabase.auth.signOut()
- Export: AuthProvider component and useAuth hook
- TypeScript strict, no any
```

### P2-2: Auth Zod Schemas
```
Generate Zod validation schemas for authentication forms.

File: src/features/auth/schemas/authSchemas.ts

Required schemas:
1. loginSchema: { email: valid email, password: min 6 chars }
2. registerSchema: { email: valid email, password: min 8 chars with at least 
   one uppercase + one number, confirmPassword: must match password, 
   fullName: min 2 chars max 50 chars }

Export each schema and its inferred TypeScript type (LoginFormData, RegisterFormData).
```

### P2-3: Auth Service
```
Generate the authentication service functions.

File: src/features/auth/services/authService.ts

Functions needed:
1. signIn({ email, password }): Promise<{ user: User, session: Session }>
   - calls supabase.auth.signInWithPassword
   - throws error if auth fails

2. signUp({ email, password, fullName }): Promise<{ user: User }>
   - calls supabase.auth.signUp
   - also inserts into public.users table: { id, email, full_name }
   - throws error if signup fails

3. signOut(): Promise<void>
   - calls supabase.auth.signOut

4. getCurrentUser(): Promise<User | null>
   - calls supabase.auth.getUser

Use the supabase client from src/lib/supabase.ts.
TypeScript strict, all functions typed.
```

### P2-4: Protected Route Component
```
Generate a ProtectedRoute component for React Router v6.

File: src/routes/ProtectedRoute.tsx

Requirements:
- Uses useAuth() from AuthContext
- If isLoading: render a full-screen loading spinner (centered, Lucide Loader2 icon)
- If no user: <Navigate to="/login" replace />
- If user: render <Outlet /> (for nested routes)
- TypeScript strict
```

### P2-5: Login Page
```
Generate a Login page component for an ERP system.

File: src/pages/auth/LoginPage.tsx

Requirements:
- Uses LoginForm component (reference it as imported, don't generate the form here)
- Layout: full-screen dark background, centered card (glass morphism style)
- Left side (desktop): product branding, name "ERP System", tagline
- Right side: LoginForm card
- Mobile: single column, form card centered
- Link to /register page
- Redirect to /dashboard if user is already logged in (useAuth check)
- Apply design system from UI_GUIDE.md (dark, glassmorphism)
- Lucide icons for branding
- Tailwind CSS only (no inline styles)
```

---

## Phase 3 — Entity CRUD Prompts

### P3-1: Entity Service (Products example — adapt for Customers, Suppliers)
```
Generate a Supabase service module for the Products entity.

File: src/features/products/services/productService.ts

Database table: products
Types: use Database['public']['Tables']['products']['Row'] for the entity type,
       and Database['public']['Tables']['products']['Insert'] for insert type.

Required functions:
1. getProducts({ userId, search, category, page, pageSize })
   - Filter by user_id (RLS will enforce, but add for clarity)
   - Optional text search on name, sku (use ilike)
   - Optional filter by category
   - Pagination: range(offset, limit)
   - Order by created_at DESC
   - Returns: { data: Product[], count: number }

2. getProductById(id: string): Promise<Product>
   - Single product by id
   - Throw if not found

3. createProduct(data: ProductInsert): Promise<Product>
   - Insert and return the created record

4. updateProduct(id: string, data: Partial<ProductInsert>): Promise<Product>
   - Update and return the updated record

5. deleteProduct(id: string): Promise<void>
   - Hard delete (or set deleted_at for soft delete)

All functions should throw errors; let TanStack Query handle error state.
Use the typed supabase client from src/lib/supabase.ts.
```

### P3-2: Entity Custom Hooks (Products example)
```
Generate TanStack Query v5 hooks for the Products feature.

File: src/features/products/hooks/useProducts.ts

Using: productService from ./productService
Query key factory pattern (use productKeys object).

Required hooks:
1. useProducts(filters: ProductFilters)
   - useQuery with productKeys.list(filters)
   - staleTime: 5 minutes
   - Returns product list + total count for pagination

2. useProduct(id: string)
   - useQuery with productKeys.detail(id)
   - Enabled only when id is truthy

3. useCreateProduct()
   - useMutation calling productService.createProduct
   - onSuccess: invalidate productKeys.lists(), show success toast
   - onError: show error toast

4. useUpdateProduct()
   - useMutation calling productService.updateProduct
   - onSuccess: invalidate productKeys.lists() + productKeys.detail(id)

5. useDeleteProduct()
   - useMutation calling productService.deleteProduct
   - onSuccess: invalidate productKeys.lists(), show success toast

Integrate with a toast system (assume useToast from src/hooks/useToast.ts exists).
TypeScript strict throughout.
```

### P3-3: Entity Zod Schema (Products example)
```
Generate Zod validation schemas for the Product entity forms.

File: src/features/products/schemas/productSchemas.ts

ProductFormSchema fields:
- name: string, min 2 chars, max 100 chars, required
- sku: string, min 2 chars, max 50 chars, uppercase transformed, required
- description: string, optional, max 500 chars
- category: string, optional
- unit_price: number, positive, min 0.01, required
- cost_price: number, positive, min 0, required
- stock_qty: integer, min 0, required
- min_stock: integer, min 0, default 0

Export: productFormSchema and ProductFormData (inferred type).
Also export productFilterSchema for the filter form.
```

### P3-4: Entity Form Component (Products example)
```
Generate a React form component for creating/editing a Product.

File: src/features/products/components/ProductForm.tsx

Props: { 
  defaultValues?: Partial<ProductFormData>, 
  onSubmit: (data: ProductFormData) => Promise<void>,
  isLoading: boolean,
  mode: 'create' | 'edit'
}

Requirements:
- React Hook Form with zodResolver(productFormSchema)
- Fields layout: 2-column grid on desktop, 1-column on mobile
  - Column 1: name, sku, category, description (textarea)
  - Column 2: unit_price, cost_price, stock_qty, min_stock
- Use Shadcn UI: Form, FormField, FormItem, FormLabel, FormControl, FormMessage
- Price fields: prefix with "$" currency indicator
- Submit button: shows spinner (Lucide Loader2) when isLoading
- Cancel button: secondary style, navigates back
- Error messages shown inline below each field
```

### P3-5: Data Table Component (Products example)
```
Generate a product data table using TanStack Table v8 and Shadcn UI Table.

File: src/features/products/components/ProductTable.tsx

Props: { data: Product[], isLoading: boolean, onEdit: (id: string) => void, 
         onDelete: (id: string) => void }

Column definitions:
- SKU: monospace font, text-tertiary
- Name: font-medium, text-primary
- Category: badge style
- Cost Price: right-aligned, currency format, monospace
- Unit Price: right-aligned, currency format, monospace  
- Stock: right-aligned, color-coded (green if > min_stock, amber if low, red if 0)
- Created At: formatted date
- Actions: Edit button (Lucide Pencil icon) + Delete button (Lucide Trash2 icon)
  Both are ghost icon buttons, visible on row hover

Include:
- Loading skeleton (show skeleton rows when isLoading)
- Empty state when data is empty
- Sticky header
- Responsive (horizontal scroll on mobile)
- Apply design system styles from UI_GUIDE.md
```

---

## Phase 4 — Transaction Module Prompts

### P4-1: Purchase Form (Multi-line Items)
```
Generate a Purchase creation form with dynamic line items.

File: src/features/purchases/components/PurchaseForm.tsx

Form structure:
Header section:
- Supplier selector: searchable combobox using Shadcn Command + Popover pattern
- Purchase date: date picker
- Notes: optional textarea

Line items section (dynamic list, useFieldArray):
- Each row: Product selector (combobox) | Quantity (number input) | 
  Unit Price (auto-filled from product.cost_price, editable) | 
  Total (auto-computed, read-only) | Delete row button
- "Add Item" button to append new empty row
- Minimum 1 line item required
- Validation: all line items must have product + quantity > 0

Footer:
- Grand Total: auto-computed sum of all line item totals, right-aligned, large
- Submit button: "Complete Purchase" (primary)
- Cancel button (secondary)

Use React Hook Form useFieldArray for the line items array.
Zod schema must validate the entire nested structure.
```

### P4-2: Stock Management Logic
```
Explain and outline (no code) how I should implement the automatic stock 
management for this ERP system using Supabase.

Context:
- purchases table has a 'status' column: 'pending' | 'completed' | 'cancelled'
- sales table has a 'status' column: 'pending' | 'completed' | 'cancelled'
- When purchase status → 'completed': increase stock for each purchase_item
- When sale status → 'completed': decrease stock for each sale_item

Questions to answer:
1. Should this be done in a Supabase database trigger or in the application service layer? 
   What are the pros/cons of each for this use case?
2. How do I write a PostgreSQL trigger function in Supabase that:
   - Fires after purchase status changes to 'completed'
   - Updates stock_qty for each related purchase_item
3. How do I handle the stock validation for sales to prevent negative stock?
4. What happens if the trigger fails midway? How do I ensure atomicity?
```

### P4-3: Sale with Stock Validation
```
Generate the sales service with stock validation logic.

File: src/features/sales/services/saleService.ts

createSaleWithItems(data: { customerId: string, items: SaleItemInput[], notes?: string })
must:
1. First, fetch current stock_qty for all product_ids in the items array
2. Validate: for each item, if item.quantity > product.stock_qty, 
   throw an error: "Insufficient stock for [product.name]. 
   Available: [stock_qty], Requested: [quantity]"
3. If validation passes, insert the sale record (status: 'pending')
4. Insert all sale_items
5. Then update sale status to 'completed' (this triggers the DB trigger for stock decrease)
6. Return the complete sale with items

Use a Supabase transaction approach (multiple queries in sequence; 
Supabase JS does not support client-side transactions, 
so use a database function via RPC for atomicity).

Explain the RPC function signature I'd need to implement in PostgreSQL 
(don't write the SQL, just describe what parameters and logic it needs).
```

---

## Phase 5 — Dashboard & Reports Prompts

### P5-1: Dashboard Stats Service
```
Generate the dashboard statistics service.

File: src/features/dashboard/services/dashboardService.ts

getDashboardStats(): fetches all KPI values for the authenticated user:
- total_products: COUNT of products where user_id = auth.uid()
- total_customers: COUNT of customers where user_id = auth.uid()
- total_suppliers: COUNT of suppliers where user_id = auth.uid()
- total_purchases: COUNT of purchases where user_id = auth.uid()
- total_sales: COUNT of sales where user_id = auth.uid()
- total_revenue: SUM of total_amount from sales where status = 'completed'

Implement this as a Supabase RPC call to a function 'get_dashboard_stats' 
(describe what parameters the DB function needs).

Return type: DashboardStats interface with all 6 fields.
Stale time should be 30 seconds (configure in the hook, not here).
```

### P5-2: Report Query Service
```
Generate the report service for the Products Report.

File: src/features/reports/services/reportService.ts

getProductReport(filters: { dateFrom?: string, dateTo?: string, category?: string })
Returns an array of objects with:
- Product fields: id, name, sku, category, unit_price, cost_price, stock_qty
- total_purchases_count: number of times this product was purchased
- total_purchased_qty: total quantity ever purchased
- total_sales_count: number of times this product was sold
- total_sold_qty: total quantity ever sold
- total_revenue: sum of sale_items.total_price for this product

This requires a JOIN across products, purchase_items, and sale_items.
Use a Supabase database view or RPC function for this (describe the approach).
Implement the frontend service function that calls it.
```

---

## General Debugging Prompts

### D-1: Supabase Query Returning Empty Array
```
My Supabase query is returning an empty array [] but I know data exists in the table.

My query:
[paste your query builder code]

Table name: [table]
RLS is enabled. My current user: [how to get auth.uid() from Supabase dashboard]
User_id on the rows: [value]

Supabase Anon key: [confirm you're using anon key, not service role]

Please help me systematically debug this. 
Start with the most likely cause (RLS) and walk me through how to test each possibility.
```

### D-2: TypeScript Type Error
```
I have a TypeScript error I can't resolve:

Error message:
[paste exact error]

Code where error occurs:
[paste code]

Relevant types:
[paste type definitions]

Please:
1. Explain why this error occurs
2. Give me 2-3 ways to fix it
3. Recommend the best fix that doesn't sacrifice type safety
```

### D-3: React Hook Form Not Submitting
```
My React Hook Form onSubmit handler is not being called when I click the submit button.
No validation errors are shown in the UI either.

Form code: [paste form component]
Zod schema: [paste schema]
Submit handler: [paste onSubmit function]

Please diagnose what might be silently failing.
Common issues to check: zodResolver setup, form not wrapped in FormProvider 
(if using useFormContext), button type not set to "submit", nested form elements.
```

### D-4: TanStack Query Not Updating After Mutation
```
After calling my mutation (createProduct), the product list is not refreshing.

Mutation code: [paste useMutation hook config]
Query code: [paste useQuery hook config]
Query key factory: [paste queryKey structure]

The invalidateQueries call I'm making: [paste]

Why might the list query not be invalidating and refetching?
Check: query key matching, whether invalidateQueries call is in onSuccess or after await,
whether the query is in a different component than where invalidation happens.
```

---

## Code Review Prompts

### CR-1: Full Feature Security Review
```
Perform a security review of this ERP feature module:

Service file: [paste]
Hook file: [paste]
Form component: [paste]

Check for:
1. Are Supabase queries filtered by user_id (even with RLS as backup)?
2. Is any sensitive data being logged or exposed in responses?
3. Are all inputs validated with Zod before reaching the service?
4. Could a user manipulate form data to bypass business logic?
5. Are error messages leaking implementation details to the client?
6. Are there any race conditions in the stock management logic?
```

### CR-2: Performance Audit
```
Audit this component for performance issues:

[paste component code]
[paste associated hook(s)]

Check for:
1. Unnecessary re-renders (missing memo, callback, stable references)
2. N+1 queries (fetching data inside loops/maps)
3. Large data sets being fetched when pagination could be used
4. Missing loading/suspense boundaries causing layout shift
5. Heavy computations that should be memoized
6. Missing TanStack Query prefetching opportunities
```

---

## Documentation Prompts

### DOC-1: Generate Component Documentation
```
Generate JSDoc documentation for this React component:
[paste component]

Include:
- Component description (what it renders, when to use it)
- @param for each prop with type and description
- @example showing how to use the component
- Any important behavior notes (side effects, event handlers)
```

### DOC-2: Generate Conventional Commit Message
```
Generate a conventional commit message for these changes:

Files changed: [list files]
Summary of changes: [describe what was added/changed/fixed]

Format: type(scope): short description
        
        longer body if needed
        
        Closes #[issue number if any]

Types: feat (new feature), fix (bug fix), refactor, docs, style, test, chore
Scope: should be the module name (auth, products, sales, dashboard, etc.)
Keep the subject line under 72 characters.
```

### DOC-3: Sprint Retrospective
```
Generate a sprint retrospective summary document for:

Sprint number: [N]
Sprint goal: [goal]
Completed features: [list]
Technical decisions made: [list key decisions]
Blockers encountered: [list]
Time estimate vs actual: [estimate vs actual]

Format as markdown with sections: 
What We Shipped, Key Decisions & Rationale, Challenges, Lessons Learned, Next Sprint Focus
```
