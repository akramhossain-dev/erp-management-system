/**
 * Auth feature — Zod validation schemas.
 * These schemas are ready for Phase 2 auth implementation.
 */
export { loginSchema, registerSchema } from "@/lib/schemas";
export type { LoginFormValues, RegisterFormValues } from "@/lib/schemas";
